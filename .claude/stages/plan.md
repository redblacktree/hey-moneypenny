---
description: Create implementation plan
---

# Plan Stage

Read the goal file at `thoughts/shared/runs/{ticket}/goal.md`.

Look for research in `thoughts/shared/research/` containing `{ticket}` in the filename. If found, read it and reference it when planning.

Use `/ff_create_plan` with the goal. Include `{ticket}` in your plan filename so later stages can find it.
